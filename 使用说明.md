# AI 中转站用量桌面悬浮窗

一个常驻桌面的小悬浮窗：定时拉取中转站的调用日志，**按模型汇总消耗金额**实时显示在桌面上，不用再进后台手动刷新。

## 一、当前配置（已填好，直接启动）

本文件夹里的 `config.json` 已经填入你的真实信息，**直接双击 `start_widget.vbs` 即可使用**。

- 站点：AIZZZ（`https://api.aizzz.xyz`，new-api 面板）
- 数据源：`GET /api/log/token`（调用日志，按模型聚合消耗）
- 刷新间隔：300 秒（5 分钟）一次（可改 `refresh_seconds`）
- 时间筛选：默认「今天」（可右键切换）
- 代理：`"proxy": "auto"` —— 自动探测 Clash 端口（7897/7890 等）

> 注意：AIZZZ 面板对大陆直连有限制，**需要开着 Clash 才能取到数据**。悬浮窗每 5 分钟请求一次，调用很克制。

## 一之二、跟着 Clash 节点切换

悬浮窗的所有请求都走 Clash 的本地代理端口（自动探测 7897 等），**你在 Clash 里切换节点后，下一次请求自动走新节点**，无需任何操作。

- 每次刷新都会**重新探测代理端口**：即使 Clash 的端口设置变了也能跟上；请求失败时也会先清缓存重新探测再试一次。
- 悬浮窗第二行会显示当前状态：如 `节点 香港-01`（显示节点名）、`代理端口 7897`（读不到节点名时）、`未检测到 Clash 代理`。
- 想要显示**节点名**：在 Clash Verge 设置里打开「启用外部控制器」（设置 → 启用 Web 面板/外部控制器）即可，悬浮窗会自动读到并显示，无需改代码。
- 如果切节点后一直报错：通常是新节点本身连不上该站点或所在地区被限制，换一个节点即可。

## 二、悬浮窗怎么用

| 操作 | 效果 |
|---|---|
| 左键按住拖动 | 移动悬浮窗位置（位置会自动记住） |
| 双击标题 | 打开中转站后台（使用日志页） |
| 右键 | 菜单：立即刷新 / 重新加载配置 / 时间筛选 / 打开后台 / 编辑配置 / 开机自启 / 退出 |
| 右键 → 时间筛选 | 今天 / 昨天 / 本周 / 本月 / 全部（切换后自动保存并立即刷新） |
| 标题前的 ● | 绿=正常刷新，黄=刷新中，红=出错了 |
| 右上角 🔓 / 🔒 | 锁定开关：未锁时可拖动可点击；**锁上后窗口穿透**——鼠标点窗口会点到桌面/下面窗口，窗口不可移动不可点击，只能点锁（🔒）来解锁 |

**任务栏托盘（右下角蓝底 $ 图标）**：
| 操作 | 效果 |
|---|---|
| 鼠标悬停 | 直接显示最新汇总（今日金额 / 各模型 Token / 更新时间），不用开窗口 |
| 左键单击 | 呼出 / 收起悬浮窗 |
| 右键 | 同悬浮窗菜单（刷新 / 时间筛选 / 打开后台 / 退出） |
| 双击 | 打开中转站后台 |

> 窗口背景与文字共用**一套透明度**（`alpha` 0.1，越小越透，默认几乎全透）；
> 窗口主体每行显示一个模型的 **消耗金额** 和 **Token 量（输入+输出汇总）**，按消耗从高到低排列；
> 标题会显示当前筛选范围，如「AIZZZ 用量 [今天]」。
> 改完 config.json 后，在悬浮窗上右键 →「重新加载配置」即可生效，不用重启。

## 三、config.json 字段说明

| 字段 | 说明 |
|---|---|
| `site_url` | 中转站面板网址（必填） |
| `api_key` | API-Key（必填） |
| `refresh_seconds` | 刷新间隔秒数，最小 5（当前 300 = 5 分钟一次） |
| `time_range` | 时间筛选：`today` 今天 / `yesterday` 昨天 / `week` 本周 / `month` 本月 / `all` 全部 |
| `proxy` | `"auto"` 自动探测 Clash 端口；`""` 直连；或写死 `"http://127.0.0.1:7897"` |
| `display_name` | 悬浮窗标题 |
| `quota_per_unit` | 额度换算：`额度数值 ÷ 该值 ≈ 金额`，AIZZZ 为 500000（美元） |
| `currency` | 金额符号 `¥` 或 `$` |
| `page_path` | 双击/右键「打开后台」跳转的路径 |
| `alpha` | 整窗不透明度 0~1（默认 0.1，越小越透）；背景与文字共用一套透明度 |
| `width` | 悬浮窗宽度（像素） |

## 四、数据来源说明

1. **唯一数据源**：`GET {site}/api/log/token`（带 `Authorization: Bearer <API-Key>`）。
   实测该站接口**忽略时间/分页参数**，每次直接返回全部可见调用记录（最近约 4 天、900+ 条）。
2. 程序拉回全部记录后**在本地按筛选范围过滤**（按每条记录的 `created_at` 判断是否落在所选时段），
   再按 `model_name` 汇总 `quota`（÷ `quota_per_unit` 500000 换算成美元金额）
   和 `prompt_tokens + completion_tokens`（Token 总量），按消耗降序展示。
3. 时间范围由 `time_range` 决定（今天=今日0点至今；昨天/本周/本月/全部同理），全部按东八区计算。
   > 限制：由于接口只返回最近约 4 天的日志，「全部」筛选的实际统计范围以接口可见记录为准；
   > 「今天 / 昨天 / 本周」在正常用量下是准确的。

## 五、安全与隐私

- `api_key` 只保存在你本机的 `config.json` 里，不会上传到任何地方。
- 数据请求直接发到中转站自己（走本地 Clash 代理），不经过第三方。
- 请勿把这个文件发给别人；如果 Key 曾经在聊天中发送过，介意的话可在后台重新生成一个再替换进 config.json。

## 六、常见问题

- **悬浮窗显示红色报错**：右键 → 立即刷新看提示，或双击 `debug_run.bat` 看详细日志；
  常见原因：Clash 没开、代理端口变了、Key 失效、调用太频繁被限流（429，等几分钟自动恢复）。
- **Clash 端口不是 7897**：把 `proxy` 改成你的实际端口，或改代码里的 `PROXY_PORTS`。
- **换站了**：把 `site_url` / `api_key` 换成新站即可；接口对不上时把网址发我适配。

## 七、本地模拟自测（可选）

不想立刻动真实账号时，可以先用本地模拟数据看效果：

```
python mock_server.py     # 启动本地模拟接口（另一个窗口）
python ai_usage_widget.py mock_config.json   # 用模拟配置启动悬浮窗
```

看到「deepseek-v4.1-flash $3.00」等按模型汇总的假数据即说明程序正常。
